using UnityEngine;
using System.Collections;

namespace MyPlatformer
{
public class ReboundiebleObj : MonoBehaviour {

	    interface IRebondieble
        {
            void ReboundFromIt(Shoot reboundeShoot);
        }
	    void Start () {
	
	    }
	
	    // Update is called once per frame
	    void Update () {
	
	    }
    }
}